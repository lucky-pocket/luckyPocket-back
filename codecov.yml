coverage:
  status:
    project:
      default:
        enabled: yes
        target: 70
        threshold: null
    patch:
      default:
        enabled: yes
        target: 85
        threshold: null